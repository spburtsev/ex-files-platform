{
  "name": "integration-testing",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "gen:traffic": "bun scripts/generate-traffic.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
